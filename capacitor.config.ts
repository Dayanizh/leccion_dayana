import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'leccion_pinela_vargas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
